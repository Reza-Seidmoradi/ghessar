import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        margin: 5,
        padding: 10,
    },
    text: {
        textAlign: 'center',
    },
    btn:{
        margin: 10,
        borderRadius: 4,
    }
});

export default styles;
